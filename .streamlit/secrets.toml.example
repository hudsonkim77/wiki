# 실제 값이 들어간 secrets.toml은 .gitignore로 커밋되지 않습니다.
# 로컬: 이 파일을 .streamlit/secrets.toml로 복사한 뒤 실제 비밀번호로 바꾸세요.
# Streamlit Cloud: 앱 설정 > Secrets 메뉴에 아래와 동일한 형식으로 직접 입력하세요(코드로 배포되지 않음).

MGMT_PASSWORD = "여기에-실제-비밀번호"
